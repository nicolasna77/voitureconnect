export interface FilterState {
  category: string;
  vehicleType: string;
  fuelType: string;
  priceMin: string;
  priceMax: string;
  yearMin: string;
  yearMax: string;
  transmission: string;
  kmMin: string;
  kmMax: string;
}
